Public Class FrontendController
    Private _picList As List(Of Semantic)

    Public Sub addImage(ByVal imgName As String,
                        ByVal img As String,
                        ByVal desc As String)

    End Sub
End Class
